{
  "name": "raasta-server",
  "version": "0.1.0",
  "description": "RAASTA / NER Route Intelligence backend (Phase 1: proxy, cache, rate-limit)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
